import 'mocha';
import { expect } from 'chai';
import strToUnitArray from '../';

describe("strToUnitArray test", function () {
  it('strToUnitArray is a function', function () {
    expect(strToUnitArray).to.be.a('function')
  })
});
